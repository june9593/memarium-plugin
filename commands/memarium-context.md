---
description: Load this project's typed memory (core rules / procedures / facts / episodes / conflicts) at the start of work, so you begin already familiar with the project.
---

Invoke the **memarium-context** skill via the `Skill` tool with `skill: "memarium:memarium-context"`.

Run this at the START of work in a project repo (before exploring code). It loads the project's accumulated memory — architecture, setup, gotchas, rules — so you don't re-learn the codebase.
